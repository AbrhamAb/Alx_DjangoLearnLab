# Django Admin Setup for Book Model

- Registered the Book model in `bookshelf/admin.py`.
- Customized admin list view with `title`, `author`, and `publication_year`.
- Added search and filter options for easier data management.
